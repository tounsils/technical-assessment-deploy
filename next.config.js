/** 
 * next.config.js — REPLACED WITH SAFE DEFAULT
 * The previous file contained obfuscated / injected content that required
 * `request` at runtime. That prevented Next from starting.
 *
 * Keep the backup at `next.config.js.bak` for inspection.
 */
 /** @type {import('next').NextConfig} */
 const nextConfig = {
   reactStrictMode: true,
 };

 module.exports = nextConfig;
